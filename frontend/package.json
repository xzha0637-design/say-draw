{
  "name": "say-draw-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "say-draw 前端:画布 + 语音 + 执行引擎",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "konva": "^9.3.20"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
