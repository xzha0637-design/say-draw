# say-draw —— AI 语音绘图工具

纯语音控制的绘图工具:用户不使用鼠标/键盘绘图,仅通过语音指令完成绘图创作(允许点击"按住说话"按钮)。

> 七牛云比赛 · 命题二:AI 语音绘图工具

## 功能(规划)

- **L1 图元绘制**:圆 / 矩形 / 线 / 三角 / 箭头 / 文字 + 颜色、大小、九宫格位置
- **L2 对象编辑**:选中、移动、改色、缩放、旋转、删除(编号角标 + 焦点指代消解)
- **L3 画布操作**:撤销 / 重做 / 清空、导出
- **L4 复杂场景**(冲刺):语音描述 → 生成可继续编辑的具名图元组
- **L5 相对定位 / L6 语音(TTS)反馈**(冲刺)

详见 [设计文档](docs/design.md)。

## 技术栈

- 前端:Vite + TypeScript + Konva
- 语音输入:Web Speech API(zh-CN);语音反馈:SpeechSynthesis
- 指令理解:Claude(Haiku 4.5 快路解析 / Opus 4.8 场景生成与多模态评审),结构化输出强制 JSON
- 后端:薄代理(转发 Claude API,密钥不进浏览器)

## 依赖说明

> 按比赛要求列明第三方依赖并标注原创部分,随开发补全。

- **第三方**:konva、@anthropic-ai/sdk、vite、typescript …(详见各模块 `package.json`)
- **原创部分**:指令文法与解析、双路路由、编号+焦点编辑模型、草图先行渲染、执行引擎等。

## 目录结构

```
/frontend   前端(画布 + 语音 + 执行引擎)
/backend    后端薄代理(转发 Claude)
/docs       设计文档
```

## 运行方式

> 随开发补全。

## Demo 视频

> 随开发补全(B站 / 云盘链接,可播放)。
